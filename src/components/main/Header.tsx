import React from "react";

import Loader from "../loader/Loader";

const Header = () => {
  return (
    <>
      <header>
        <nav></nav>
      </header>
    </>
  );
};

export default Header;
